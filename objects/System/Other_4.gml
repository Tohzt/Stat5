/// @description INITIALIZE PLAYER


for (var tX = 0; tX < MAP_W; tX++){
	for (var tY = 0; tY < MAP_H; tY++)
	{	/*
		// OBSTACLE TILES
		var mapTile = global.theMap[# tX,tY];
		if (mapTile[TILE.SPRITE] > 0) {
			var pX = tX * TILE_W;
			var pY = tY * TILE_W;
			var inst_block = instance_create_layer(
				pX,
				pY,
				"Terrain",
				oBlock
			);
			
			selected_x = tX;
			selected_y = tY;
			
			with (inst_block) {
				gPos_x = other.selected_x;
				gPos_y = other.selected_y;
				var tileData = global.theMap[# gPos_x, gPos_y];
				image_index = tileData[TILE.SPRITE]-1;
			}
			
		}*/
	}
}